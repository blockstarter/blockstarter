// Generated by LiveScript 1.5.0
(function(){
  var bip38, wif, ref$, out$ = typeof exports != 'undefined' && exports || this;
  bip38 = require('bip38');
  wif = require('wif');
  ref$ = out$;
  ref$.encrypt = function(string, key){
    var decoded;
    decoded = wif.decode(string);
    return bip38.encrypt(decoded.privateKey, decoded.compressed, key);
  };
  ref$.decrypt = function(string, key){
    var progress, decryptedKey;
    progress = function(status){
      return console.log(status.percent);
    };
    decryptedKey = bip38.decrypt(string, key, progress);
    return wif.encode(decryptedKey.privateKey, decryptedKey.compressed);
  };
}).call(this);
