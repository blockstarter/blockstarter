// Generated by LiveScript 1.5.0
(function(){
  var ethUtil, crypto;
  ethUtil = require('ethereumjs-util');
  crypto = require('crypto');
  module.exports = function(type){
    var privKeyBuff, addrBuff, pubBuff;
    privKeyBuff = crypto.randomBytes(32);
    addrBuff = ethUtil.privateToAddress(privKeyBuff);
    pubBuff = ethUtil.privateToPublic(privKeyBuff);
    switch (type) {
    case 'test':
      return null;
    default:
      return {
        address: '0x' + addrBuff.toString('hex'),
        privateKey: privKeyBuff.toString('hex'),
        'public': pubBuff.toString('hex')
      };
    }
  };
  module.exports.verify = function(str){
    var pattern;
    pattern = '0xde0B295669a9FD93d5F28D9Ec85E40f4cb697BAe';
    return pattern.length === str.length && str.substr(0, 2) === "0x";
  };
}).call(this);
